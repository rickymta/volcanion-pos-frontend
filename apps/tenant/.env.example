VITE_API_BASE_URL=https://localhost:63283/api/v1

# Dev fallback: dùng khi chạy plain localhost:3000 (không có subdomain)
# Trên production subdomain được đọc tự động từ hostname (tenant-a.pos.vn)
# Để test local: mở http://tenant-a.localhost:3000 (không cần env này)
# Hoặc set giá trị dưới đây để dùng plain http://localhost:3000
VITE_DEV_TENANT_SLUG=
